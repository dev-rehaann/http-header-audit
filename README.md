# HTTP Header Audit

A small, beginner-friendly HTTP security-header grader.

Milestone 1 contains the pure rule evaluation and scoring logic. Network access
and the command-line interface will be added in later milestones.

```bash
uv sync --extra dev
uv run pytest
```

