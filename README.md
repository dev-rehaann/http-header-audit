# HTTP Header Audit

A small, beginner-friendly HTTP security-header grader.

Milestone 2 adds one synchronous HTTP request, redirect handling, and mocked
network tests. The command-line interface will be added next.

```bash
uv sync --extra dev
uv run pytest
```
